import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Italian Phonetic Alphabet con Nomi di Città",
  description: "Learn the Italian phonetic alphabet using city names! Un tool interattivo per convertire testo nell'alfabeto fonetico italiano, usando città come Ancona, Bologna, e Milano per each letter. Perfect for learners, travelers, o chiunque voglia migliorare la communicatione in italiano.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
